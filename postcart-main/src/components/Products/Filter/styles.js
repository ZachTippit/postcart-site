import {makeStyles} from '@material-ui/core/styles';

export default makeStyles(() => ({
    filterBar: {
        padding: '1rem'
    },

    filterText: {
        fontWeight: "bold"
    },

    resetButton: {
        backgroundColor: '#f4fdf4',
        marginTop: '30px'
    }
}));